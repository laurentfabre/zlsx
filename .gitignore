.zig-cache/
zig-out/
.DS_Store
**/.DS_Store
# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
build/
dist/
# Knowledge base — private working vault (architecture notes + collaboration prefs).
# Track individual notes with `git add -f` if you want them in the repo.
docs/kb/

# Z2 co-import gate build artifacts (tests/consumer is its own package)
tests/consumer/zig-out/
tests/consumer/.zig-cache/

# bench_ci.sh's working directory: rebuilt binaries, emitted fixtures and
# the run's own hyperfine JSON. A baseline worth keeping goes to
# tests/bench/baseline_*.json, which is committed.
bench-out/

# Workspace credentials (Databricks PAT etc.) — never commit
.env
.env.*
